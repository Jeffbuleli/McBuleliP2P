# Optional Render Blueprint — import in Dashboard → Blueprints.
# Web service (McBuleli Next.js) is usually created separately.
# Either enable BOT_CRON_INLINE=1 on the web service OR use mcbuleli-bots-tick below.

services:
  - type: cron
    name: mcbuleli-bots-tick
    runtime: node
    region: oregon
    plan: starter
    schedule: "*/5 * * * *"
    buildCommand: "true"
    startCommand: node scripts/cron-bots-tick.mjs
    envVars:
      - key: MCBULELI_API_URL
        sync: false
      - key: CRON_SECRET
        sync: false

  - type: cron
    name: mcbuleli-ai-relay
    runtime: python
    region: oregon
    plan: starter
    schedule: "*/1 * * * *"
    rootDir: services/mcbuleli-ai-trading
    buildCommand: pip install -r requirements.txt
    startCommand: python scripts/relay_all_instances.py
    envVars:
      - key: MODE
        value: SIGNAL_ONLY
      - key: MCBULELI_API_URL
        sync: false
      - key: MCBULELI_CRON_SECRET
        sync: false
      - key: MCBULELI_INSTANCE_ID
        value: ""
      - key: TWITTER_ENABLED
        value: "1"
      - key: TWITTER_BEARER_TOKEN
        sync: false
      - key: X_LLM_ENABLED
        value: "1"
      - key: OPENAI_API_KEY
        sync: false
      - key: OPENAI_MODEL
        value: gpt-5.5
      - key: SIGNAL_MIN_EDGE
        value: "15"
      - key: X_LLM_BLEND_WEIGHT
        value: "0.55"

  # Alternative: continuous worker (uncomment and disable cron above if preferred).
  # - type: worker
  #   name: mcbuleli-ai-relay-worker
  #   runtime: python
  #   region: oregon
  #   plan: starter
  #   rootDir: services/mcbuleli-ai-trading
  #   buildCommand: pip install -r requirements.txt
  #   startCommand: python scripts/relay_loop.py
  #   envVars:
  #     - key: MODE
  #       value: SIGNAL_ONLY
  #     - key: INTERVAL_SEC
  #       value: "60"
  #     - key: MCBULELI_API_URL
  #       sync: false
  #     - key: MCBULELI_CRON_SECRET
  #       sync: false
  #     - key: MCBULELI_INSTANCE_ID
  #       value: ""
