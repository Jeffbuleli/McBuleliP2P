# Install: /etc/nginx/conf.d/00-mcbuleli-client-ip.conf
# Must live in http{} context (conf.d is included there).

map $http_cf_connecting_ip $mcb_client_ip {
  default $remote_addr;
  "~."    $http_cf_connecting_ip;
}
