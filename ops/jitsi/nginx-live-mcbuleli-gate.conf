# Référence — le script apply-nginx-live-gate.sh installe automatiquement
# /etc/nginx/snippets/mcbuleli-live-gate.conf et l'include dans le vhost.
#
# Ne pas coller proxy_pass localhost:8080 (incorrect sur install Jitsi Debian).
# Utilisez: bash apply-nginx-live-gate.sh
