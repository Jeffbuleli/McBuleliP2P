/**
 * Bandeau vert retiré — le watermark vidéo est géré par mcbuleli-custom.css (style Jitsi).
 */
(function () {
  var old = document.getElementById("mcbuleli-prejoin-banner");
  if (old) old.remove();
})();
